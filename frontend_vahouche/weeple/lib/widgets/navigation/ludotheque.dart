import 'package:flutter/material.dart';

class Ludotheque extends StatelessWidget {
  const Ludotheque({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Ludothèque"));
  }
}
